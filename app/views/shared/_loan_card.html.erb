<% user = owner_pic ? loan.book.user : loan.user %>
<div class="col-xs-3 col-md-2">
  <div class="flex-container-centered-vh list-row-130">
    <% profile_pic = loan.user_id == current_user.id ? "image_tag 'profilepic_4'" : user.profile.profile_picture %>
    <div class="loan-requester avatar-square", style="background-image: url(<%= image_path profile_pic %>);">
    </div>
    <div>
      <% name = loan.user_id == current_user.id ? loan.borrower_name_manual : user.profile.first_name%>
    </div>
  </div>
</div>

<div class="col-xs-9 col-md-6">
  <div class="flex-container-centered-v list-row-130">
    <div>
      <strong><%= loan.book.title %></strong> (<%= loan.book.language %>)
      <em><%= loan.book.author %></em>

      <div class="padded-top-2em">
        <%= loan.status.capitalize %>: <%= loan.last_action.strftime('%d/%m/%Y') %>
      </div>

    </div>
  </div>
</div>
